import 'package:flutter/material.dart';

class ProductsProvider extends StatelessWidget {
  const ProductsProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ProductsProvider'),
      ),
    );
  }
}
